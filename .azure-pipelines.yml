jobs:
- template: .azure-pipelines-job.yml
  parameters:
    NAME: Linux
    VMIMAGE: ubuntu-16.04

- template: .azure-pipelines-job.yml
  parameters:
    NAME: Windows
    VMIMAGE: vs2017-win2016

- template: .azure-pipelines-job.yml
  parameters:
    NAME: macOS
    VMIMAGE: macOS-10.13
